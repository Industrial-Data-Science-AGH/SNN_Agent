#!/bin/sh
# SNN Agent: GitHub alone may update master via reviewed PR.
while read -r local_ref local_sha remote_ref remote_sha; do
    if [ "$remote_ref" = "refs/heads/master" ]; then
        echo "Blocked: direct push to master is forbidden. Open a GitHub PR." >&2
        exit 1
    fi
done
exit 0
