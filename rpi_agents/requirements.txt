# rpi_agents runtime dependencies — RPi 5 / Raspberry Pi OS (Bookworm, 64-bit)
#
# Pi-only packages (picamera2, gpiozero, lgpio) are installed via apt +
# --system-site-packages venv on the Pi.  They will NOT install on macOS.
# For Mac dev / CI (unit tests + eval harness): gpiozero, pytest, numpy, python-dotenv.
#
# Install on the Pi:
#   sudo apt install -y python3-picamera2 python3-lgpio
#   python -m venv --system-site-packages .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Install on Mac (unit tests + eval harness):
#   pip install gpiozero pytest numpy python-dotenv opencv-python-headless google-genai requests

# --- Pi-only (apt-managed; listed for documentation) ---
# picamera2          # CSI camera capture (apt: python3-picamera2)
# lgpio>=0.2         # Pi 5 RP1-compatible GPIO backend (apt: python3-lgpio)

# --- pip-installable (Pi + Mac) ---
gpiozero>=2.0
opencv-python-headless>=4.9
google-genai>=0.8
python-dotenv>=1.0
numpy>=1.26
pytest>=8.0
requests>=2.31  # agent/cloud_sync.py -- F03, ADR-0014/0015
